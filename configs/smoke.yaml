seed: 42
data_root: ./data
output_root: ./outputs
dataset: pet
image_size: 96
max_samples: 300
models: [unet]
methods: [baseline, DASA_EM]
batch_size: 16
baseline_epochs: 1
em_rounds: 1
epochs_per_m_step: 1
lr: 0.001
weight_decay: 0.0001
mc_passes: 2
budget_strength: 3.0
num_workers: 2
save_checkpoints: true
